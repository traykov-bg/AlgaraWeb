@model Algara.Web.ViewModels.LoginViewModel

@{
    ViewData["Title"] = "Вход";
}

<h2>Вход</h2>

<form asp-action="Login" asp-route-returnUrl="@ViewBag.ReturnUrl" method="post" id="loginForm">
    <input asp-for="TimeZoneOffset" type="hidden" id="timeZoneOffset" />

    <div class="form-group">
        <label asp-for="Email"></label>
        <input asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Password"></label>
        <input asp-for="Password" class="form-control" type="password" />
        <span asp-validation-for="Password" class="text-danger"></span>
    </div>

    <div class="form-group d-flex justify-content-between align-items-center">
        <div>
            <input asp-for="RememberMe" />
            <label asp-for="RememberMe">Запомни ме</label>
        </div>
        <a asp-action="ForgotPassword" class="algara-link-accent small">Забравена парола?</a>
    </div>
    <button type="submit" class="btn btn-primary">Вход</button>

    <div class="mt-3">
        <span class="text-muted small">Нямаш акаунт?</span>
        <a asp-action="Register" class="algara-link-accent">Регистрирай се</a>
    </div>
</form>

@section Scripts {
    <script>
        // Задаваме TimeZoneOffset точно преди изпращане на формата, за да е сигурно.
        // querySelector по name е надежден — не зависи от генерирания id.
        // getTimezoneOffset() връща минути ЗАД UTC (UTC+2 → -120), затова го негираме.
        (function () {
            var form = document.getElementById('loginForm');
            function setOffset() {
                var field = form.querySelector('[name="TimeZoneOffset"]');
                if (field) field.value = -new Date().getTimezoneOffset();
            }
            setOffset();                        // при зареждане на страницата
            form.addEventListener('submit', setOffset); // и точно преди submit
        })();
    </script>
}